Content-Type: text/x-zim-wiki
Wiki-Format: zim 0.6
Creation-Date: 2022-10-15T00:55:25+08:00

====== 进程、端口 ======
Created 星期六 15 十月 2022

ps -ef | grep 进程名

ss -tulnp：看端口监听、TCP连接状态、对应进程。现在一般优先用ss替代netstat。
   -n, --numeric       don't resolve service names
   -r, --resolve       resolve host names
   -a, --all           display all sockets
   -l, --listening     display listening sockets
   -p, --processes     show process using socket
   -s, --summary       show socket usage summary
   -t, --tcp           display only TCP sockets
   -u, --udp           display only UDP sockets

netstat -nap | grep 进程pid
netstat -nap | grep 端口号

lsof -i :端口号
lsof -i TCP:22
(list open files 列出所有打开的文件；linux下所有东西都是文件，比如socket，file，pipe等等，可用来查询特定目录下打开的文件， 特定进程打开的文件，指定用户打开的文件，查询特定端口等等，可以说isof涵盖了ss命令的功能。)
