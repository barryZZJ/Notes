Content-Type: text/x-zim-wiki
Wiki-Format: zim 0.6
Creation-Date: 2022-10-20T17:17:28+08:00

====== ffmpeg ======
Created 星期四 20 十月 2022

https://ffmpeg.org/
https://ffmpeg.org/ffmpeg.html

-y                  overwrite output files

输出到pipe
	-f muxer  # ffmpeg -muxers查看所有的muxer，如wav, eac3，并且要在-i后面
	- 表示输出到pipe

封装
https://norm.press/archives/26.html

lossless codec like -c:v ffv1 which preserves the quality of the input video.

裁剪视频：(不能精确到毫秒不知道为什么）
''ffmpeg -ss 00:00:00 -t 00:00:30 -i test.mp4 -c copy output.mp4''
	-ss start time offset Format: [HH:]MM:SS[.m...]  or ss[.m...][s|ms|us] 可以是时分秒毫秒，也可以是秒数. '55' '0.2' '200ms'
	-sseof set the start time offset relative to EOF
	-t duration 可以是时分秒毫秒，也可以是秒数 (不设置则默认到结尾) 
	-to stop time
	-i video file (要写在-ss之后，可以提高效率)
	-report 保存日志到txt
	-c codec for both video and audio

-map 指定输入流

倍速播放
视频的PTS系数为 1/倍速
e.g. 2倍速：
ffmpeg -i input.mkv -filter_complex "[0:v]setpts=0.5*PTS[v];[0:a]atempo=2.0[a]" -map "[v]" -map "[a]" output.mkv

拼接：
https://www.cnblogs.com/huojinfeng/articles/13167730.html
(方式三中的单引号改成双引号）
法1：（会重新编码）
ffmpeg -i 1.mp4 -i 2.mp4 -i 3.mp4 -filter_complex "[0:v] [0:a] [1:v] [1:a] [2:v] [2:a] concat=n=3:v=1:a=1 [v] [a]" -map "[v]" -map "[a]"  output.mp4
法2：（要求片源格式完全一致，不重新编码，经测试时间可能会不精确）
(echo file '1.mp4' & echo file '2.mp4' )>list.txt
ffmpeg -safe 0 -f concat -i list.txt -c copy output.mp4
法3：（要求片源格式完全一致，不重新编码，同上不精确）
ffmpeg -i input1.mp4 -c copy -bsf:v h264_mp4toannexb -f mpegts intermediate1.ts
ffmpeg -i input2.mp4 -c copy -bsf:v h264_mp4toannexb -f mpegts intermediate2.ts
ffmpeg -i "concat:intermediate1.ts|intermediate2.ts" -c copy -bsf:a aac_adtstoasc output.mp4


提取视频
ffmpeg -i test.mp4 -c:v copy out.mp4

提取音频
ffmpeg -i test.mp4 -c:a copy -vn output.aac
ffmpeg -i input-video.avi -vn -acodec copy output-audio.aac
-vn is no video.
-acodec copy says use the same audio stream that's already in there. (can be 'aac')(or -c:a copy)

音视频封装：
ffmpeg -i video.mp4 -i audio.aac -c:v copy -c:a copy -map 0:v:0 -map 1:a:0 output.mp4
ffmpeg -i video.mp4 -i audio.mp3 -c:v copy -c:a aac -map 0:v:0 -map 1:a:0 output.mp4

音频转码
https://www.rondochen.com/convert-mkv-to-mp4-with-ffmpeg/
ffmpeg -i xxx.eac -c:a aac output.aac

生成m4a
ffmpeg -i inputvideo -vn -c:a aac out.m4a

编码格式转换
https://ffmpeg.org/ffmpeg.html#Video-and-Audio-file-format-conversion
ffmpeg -i input.ts out.mp4

调整比特率(不加单位默认为bps)：
constant:
-b:v 4000k
variable bitrate with constant quality:
-crf 23

-maxrate xxx 限制最大比特率

x265编码：
-c:v libx265 -vtag hvc1
nvidia gpu加速 -vcodec hevc_nvenc
amd gpu加速 -c:v hevc_amf 


所有输出均在stderr；
出错时returnCode不为0.

js版
https://github.com/Kagami/ffmpeg.js
