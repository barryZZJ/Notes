Content-Type: text/x-zim-wiki

https://blog.csdn.net/weixin_41305441/article/details/85003066

1. download content
function downFile(content, filename) {
    // 创建隐藏的可下载链接
    var eleLink = document.createElement('a');
    eleLink.download = filename;
    //eleLink.style.display = 'none';
    // 字符内容转变成blob地址
    var blob = new Blob([content]);
    eleLink.href = URL.createObjectURL(blob);
    // 触发点击
    //document.body.appendChild(eleLink);
    eleLink.click();
    // 然后移除
    //document.body.removeChild(eleLink);
};

2. download url
function download(url, filename) {
  fetch(url)
    .then(response => response.blob())
    .then(blob => {
      const link = document.createElement("a");
      link.href = URL.createObjectURL(blob);
      link.download = filename;
      link.click();
  })
  .catch(console.error);
}

download("https://get.geojs.io/v1/ip/geo.json","geoip.json")
download("data:text/html,HelloWorld!", "helloWorld.txt");

3.download url with <a> tag with download attribute <a href="/test.pdf" download>
        var button = document.createElement( 'a' );
        button.href = url;
        button.target = '_blank';  // 在新标签页打开
        button.download = filename;  // 设置下载文件的名字，可设为空字符串，使用默认名字
        button.innerHTML = "⥥";  // 设置图标
        button.title = "Download";
点击后会自动下载，但必须是same origin，否则会跳转到url，设置的文件名也没有了，需要通过右键保存。
要想解决，需要套一层方法2（button的onclick事件触发方法2）