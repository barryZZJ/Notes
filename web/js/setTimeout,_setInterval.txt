Content-Type: text/x-zim-wiki

延时（异步）
只运行一次：
window.setTimeout( func , msec);

重复运行：
setInterval(function, millisec) 
会不停地调用函数，直到 clearInterval() 被调用或窗口被关闭。
由 setInterval() 返回的 ID 值可用作 clearInterval() 方法的参数。