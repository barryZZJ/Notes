Content-Type: text/x-zim-wiki

// css code
const css = `
  .someClsName{
    ...
  }

  .others{
    
  }
`;
let text = document.createTextNode(css);

var sty = document.createElement("style");
sty.type = "text/css";
sty.appendChild(text);
// 手动添加
el.appendChild(sty); 
// 或通过classname自动适配对应格式