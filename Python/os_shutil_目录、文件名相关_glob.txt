Content-Type: text/x-zim-wiki
Wiki-Format: zim 0.6
Creation-Date: 2022-09-02T14:45:14+08:00

====== 文件处理 ======
shutil https://docs.python.org/3.9/library/shutil.html
.copy(old,new) 可以是文件夹
.copyfile(old,new) 只能是文件
.copytree(old, new) 只能是目录
.move(old, new)
.which
.rename(src, dst)

os
.remove() 删除文件
.rmdir() 删除空文件夹
.removedirs()  删除路径上每一级空文件夹
.rename(src, dir)


open:模式 https://www.runoob.com/python/file-methods.html
r: 指针在开头。不存在报FileNotFoundError
w: 删除原有内容，指针在开头。不存在则创建
a: 指针在结尾。不存在则创建
+: 既可读也可写，不改变指针位置
https://blog.csdn.net/ztf312/article/details/47259805
r+:
# read ...
# write from start
f.seek(0)
f.truncate(0)
f.write()

.access 检查是否有访问权限，在open之前使用 -> bool (不适用于正在复制的文件
https://docs.python.org/3/library/os.html
os.access("path", os.W_OK)
F_OK, R_OK, W_OK, X_OK : existence, readability, writability and executability
（文件不存在会返回False）

os.system(cmd) 执行指令 （更强大的是subprocess）
	一般无报错返回0
	os.system("pause") 返回状态码
	os.system('cls') 清屏
	
os.popen() 返回控制台输出内容 (考虑使用subprocess)
获取控制台输出
cmd = 'ping baidu.com'
r = os.popen(cmd)
for line in r.readlines():
	print (line)
或print(r.read())

.mkdir()
.makedirs('多级路径', exist_ok=True)

获取当前活动目录:
os.getcwd()
获取当前文件所在目录:
os.path.dirname(__file__) 不带结尾\

os.path （考虑使用pathlib.Path [[Path]]）
.exists()

.getsize() 文件字节数（实际大小，非占用空间）

.splitext(filepath:文件路径)：-> (文件所在目录+文件名, .后缀名)
.splitext(path:目录路径)：-> (目录路径, 空)
.split(filepath:文件路径) -> (文件目录（不带结尾/）, 文件名带后缀)
.split(filepath:目录路径) -> (上一级目录（不带结尾/）, 最后一级目录名）
.basename(path) 文件名带后缀 或 最后一级路径
.dirname(path) 文件所在目录 或 父目录
.splitdrive() -> (盘符:, [[/路径]])
.commonpath(list) 求公共路径，一定会返回合法路径
.commonprefix(list) 求公共父路径（字符级，可能会返回不合法路径）

去除文件后缀名：
# 传入整个路径：
os.path.splitext(os.path.basename(filepath))[0]
# 只含文件名：
os.path.splitext(filename)[0]
或
'.'.join(filename.split('.')[:-1])

os.sep = "\\"

#只遍历根目录下所有目录和文件：
os.scandir() 同listdir，但返回entry生成器
for entry in os.scandir(path):
	entry: os.DirEntry
	entry.name # 只有文件名和后缀名
	entry.path # 完整目录带文件和后缀名
	entry.is_dir()
	entry.is_file()

os.listdir(path) 返回一个数组，只包含根目录下所有目录和文件名，随机顺序。

#递归遍历目录下的所有目录和文件：
（walk返回生成器，可以用next遍历，第一个就是根目录下的信息）
不想进入某个目录：
in-place修改dirs，如dirs.remove('CVS') # don't visit CVS directories。#直接给dirs赋值不行
或：if root不满足条件，直接dirs.clear()+continue
for root,dirs,files in os.walk(r"D:\test"):
	for file in files:
		#获取文件所属目录
		print(root)
		#获取文件路径
		print(os.path.join(root,file))
	for dir in dirs:
		#获取目录的名称
		print(dir)
		#获取目录的路径
		print(os.path.join(root,dir))


指定遍历顺序：sorted()
numbers = re.compile(r'(\d+)')
def numericalSort(value):
	parts = numbers.split(value)
	parts[1::2] = map(int, parts[1::2])
	return parts

os.writelines(iterable) 需要手动加换行符，否则默认就是把每个元素依次写入

pathlib
https://docs.python.org/3/library/pathlib.html

环境变量
os.environ
os.environ.get('PATH') -> str
os.get_exec_path()

glob模块查找文件路径用
支持转义字符：* ? []
https://docs.python.org/zh-cn/3/library/glob.html
glob.glob(pathname, *, root_dir=None, dir_fd=None, recursive=False, include_hidden=False) -> [str]
返回路径列表，可绝对可相对，取决于输入
glob.glob('./[0-9].*') # ['./1.gif', './2.txt']

.iglob -> iterator

os._exit(0)  发起一个systemcall，直接退出程序。需要状态码，0是无异常，其他非零数视作异常。
主进程一般不用，无法处理一些后续处理。
