Content-Type: text/x-zim-wiki
Wiki-Format: zim 0.6

{{{code: lang="python3" linenumbers="False"
try:

except .. as err:
except (Err1, Err2) as err:
	# 发生错误时执行
	# 就算含continue、break等指令也会先执行finally再执行
	# err会是括号内的其中一种
	# 进行处理后也可以直接写raise，会重新抛出该错误（信息仍是原来报错的地方）
	# 要想在其他地方raise：http://nedbatchelder.com/blog/200711/rethrowing_exceptions_in_python.html
except Exception as e:
	# 所有错误，兜底
	type(ex).__name__ # 显示错误类型, type: str
else:
	# 无错误时执行
finally:
	# 收尾工作，无论是否错误均执行。
}}}


# 打印详细的traceback
https://docs.python.org/3.9/library/traceback.html
https://www.geeksforgeeks.org/how-to-print-exception-stack-trace-in-python/
import traceback
traceback.print_exc()

.format_exc() -> str

自定义exception
https://www.programiz.com/python-programming/user-defined-exception
继承Exception
在init中添加自定义参数，然后调用super即可
