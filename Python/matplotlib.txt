Content-Type: text/x-zim-wiki
Wiki-Format: zim 0.6

doc:
https://matplotlib.org/stable/

Examples: https://matplotlib.org/stable/gallery/index.html

import matplotlib.pyplot as plt

https://matplotlib.org/3.5.0/api/pyplot_summary.html

保存：
plt.savefig('destination_path.eps', format='eps') 矢量图最高清
或pdf

Examples ： https://matplotlib.org/stable/gallery/index.html

解决占用内存过多：
1. 关闭交互模式
import matplotlib
matplotlib.use('agg')
import matplotlib.pyplot as plt
2. 绘制完毕后plt.clf() + plt.close()手动清除

图片大小：
fig = plt.figure(figsize=(6, 4.8)) # 600*480
要想使用cm为单位需要乘以一个比例:
width = 11 * 0.393701 # width = 11cm

ax:
1. fig, ax = plt.subplots(figsize=(3, 3))
2. fig = plt.figure(figsize=())
   ax = fig.add_subplot()

参数：
zorder=1 修改覆盖顺序，小的在下面，默认第一个在最下面。
alpha=0.1 设置透明度
linewidth /lw: float 线的粗细
linestyle /ls {'-', '--', ':', '-.'} https://matplotlib.org/stable/gallery/lines_bars_and_markers/linestyles.html?highlight=linestyle
marker 表示的是标记的样式，默认的是'o' https://matplotlib.org/3.5.0/api/markers_api.html#module-matplotlib.markers
markersize /ms
edgecolor
hatch 填充样式
colors: https://matplotlib.org/stable/gallery/color/color_demo.html
{'b', 'g', 'r', 'c', 'm', 'y', 'k', 'w'}
 {'tab:blue', 'tab:orange', 'tab:green', 'tab:red', 'tab:purple', 'tab:brown', 'tab:pink', 'tab:gray', 'tab:olive', 'tab:cyan'}
antialiased 抗锯齿
label: str 图例显示的字符串

折线图/散点图：plot
https://matplotlib.org/3.5.0/api/_as_gen/matplotlib.pyplot.plot.html
https://matplotlib.org/3.5.0/api/markers_api.html

fill_between: https://matplotlib.org/stable/gallery/lines_bars_and_markers/fill_between_demo.html

Text 文字:
  https://matplotlib.org/stable/api/_as_gen/matplotlib.pyplot.text.html 只能画在数据区域
  va = {'top', 'center', 'bottom'}
  ha = {'left', 'center', 'right'}

Annotations 标注： 可以画在整个图内
  tutorials: https://matplotlib.org/stable/tutorials/text/annotations.html
  api: https://matplotlib.org/stable/api/_as_gen/matplotlib.axes.Axes.annotate.html
  demo:
  https://matplotlib.org/stable/gallery/text_labels_and_annotations/annotation_demo.html
  https://matplotlib.org/stable/gallery/index.html#text-labels-and-annotations

ax.annotate('figure pixels', xy=(10, 10), xycoords='figure pixels')
ax.annotate('text', xy=(x, y), xycoords='data', xytext=(10, 10), textcoords='offset pixels')
  arrowprops:FancyArrowPatchArgs=dict(arrowstyle="->") 
	arrowsylte:str or ArrowStyle. -, <-, ->, <->, <|-, -|>, <|-|>, simple, fancy, wedge. widthA=1.0, widthB=1.0
		ArrowStyle: https://matplotlib.org/stable/api/_as_gen/matplotlib.patches.ArrowStyle.html
	connectionstyle: angle, angle3, arc, arc3, bar
https://matplotlib.org/stable/tutorials/text/annotations.html?highlight=connectionpatch#annotating-with-arrow
https://matplotlib.org/stable/api/_as_gen/matplotlib.patches.ArrowStyle.htm

coordinate system:
'figure points'   : points from the lower left corner of the figure
'figure pixels'   : pixels from the lower left corner of the figure
'figure fraction' : (0, 0) is lower left of figure and (1, 1) is upper right
'axes points'     : points from lower left corner of axes
'axes pixels'     : pixels from lower left corner of axes
'axes fraction'   : (0, 0) is lower left of axes and (1, 1) is upper right
'offset points'   : Specify an offset (in points) from the xy value
'offset pixels'   : Specify an offset (in pixels) from the xy value
'data'            : use the axes data coordinate system


plot types: https://matplotlib.org/stable/plot_types/index.html
plot  折线图
scatter         散点图
bar           条形图（注意横向条形图的y轴是从下网上的，与正常条形图的例子相反） https://zhuanlan.zhihu.com/p/25128216
   y = np.arange(datasize)
   height = 0.2
   #bars = 2
   #total_height = height*bars
   #y = y-(total_height-height)/2
   plt.barh(y+height, a[::-1], height=height)
   plt.barh(y, b[::-1], height=height)
   plt.yticks(y+height/2)
   auto_text_x()

   ax.invert_yaxis()可以逆向
plt.hist()	直方图 https://matplotlib.org/3.5.0/api/_as_gen/matplotlib.pyplot.hist.html
plt.pie()	饼图
plt.contour()	等高线图
plt.boxplot()	箱型图  https://matplotlib.org/stable/api/_as_gen/matplotlib.pyplot.boxplot.html
plt.violinplot()提琴图  https://matplotlib.org/stable/api/_as_gen/matplotlib.pyplot.violinplot.html
				https://matplotlib.org/stable/gallery/statistics/customized_violin.html#sphx-glr-gallery-statistics-customized-violin-py

画直线：
vlines, hlines：指定线段长度
axvline, axhline：默认横跨一整个图

跨子图画线：
1. ConnectionPatch https://matplotlib.org/stable/tutorials/text/annotations.html#using-connectionpatch
   api:   https://matplotlib.org/stable/api/_as_gen/matplotlib.patches.ConnectionPatch.html
   con = ConnectionPatch((x1,y1), (x2,y2), coordsA: str='data', coordsB: str='data', axesA=ax1, axesB=ax3, ls='--', color=colorbsh)
   fig.add_artist(con)
2. vlines, hlines, axvline, axhline 把min, max超出0, 1, 并使用参数clip_on=False, zorder大于其他图


条形图添加数值：
def auto_text(rects):
	for rect in rects:
		ax.text(rect.get_x(), rect.get_height(), rect.get_height(), ha='left', va='bottom')
rect = plt.bar()
auto_text(rect)

折线图添加数值：
def auto_text(ax, xs, ys):
	for x, y in zip(xs, ys):
		if y != np.inf:
			ax.text(x, y, y, ha='center', va='bottom')

def auto_text(ax, xs, ys, text=None, kwargs=dict(ha='left', va='bottom'), decimal=3):
	for x, y in zip(xs, ys):
		if y != np.inf:
			if text:
				ax.text(x, y, text, **kwargs)
			else:
				ax.text(x, y, np.round(y,decimal), **kwargs)


def auto_text_x(ax, xs, ys, texts=None, kwargs=dict(ha='left', va='bottom')):
	if texts is None:
		texts = [None] * len(xs)
	for x, y, text in zip(xs, ys, texts):
		if x != np.inf:
			if text is not None:
				ax.text(x, y, '%.3f' % text, **kwargs)
			else:
				ax.text(x, y, '%.3f' % x, **kwargs)

图例 Legend https://matplotlib.org/stable/api/_as_gen/matplotlib.pyplot.legend.html#matplotlib.pyplot.legend
在plot中设置label参数，指定其图例名
显示图例：plt.legend() plt.legend(label, loc = 0, ncol = 2)
loc 设置图例显示位置
ncol 设置显示的列数，当要表示的线段特别多的时候会有用

获取当前axis: ax = plt.gca()
轴长度plt.axis(xmin, xmax, ymin, ymax)
显示网格 .grid(True)
轴分布plt.yscale('linear')

坐标轴ticks(小竖线)的位置：
   xticks(arr, labels, rotation, ha='center', va='top'), yticks()
子图坐标轴标签：
   ax.set_xticks(arr, labels=None, font, fontsize, **kwargs),  设置ticks(小竖线)的位置
   ax.set_xticklabels(labels, **kwargs), 
   ax.set_xlabel()  
   ax.tick_params()  https://matplotlib.org/stable/api/_as_gen/matplotlib.axes.Axes.tick_params.html
坐标轴最值：xlim(), ylim()
坐标轴缩放：xscale(), yscale()
坐标轴标题：xlabel(), ylabel()  loc={'left','center','right'}, labelpad=float (设置负数可以更靠近x轴）
					plt.rcParams["xaxis.labellocation"] = 'right'  全局设置xlabel位置
手动调整label位置：transform 可以指定不同的对齐方式，坐标可以为负数，也可超过1
ax.yaxis.set_label_coords(x, y, transform=None)
ax.xaxis.set_label_coords(x, y, transform=None)
默认对齐方式为 'axes fraction'   : (0, 0) is lower left of axes and (1, 1) is upper right

#adjust x-axis label position 
ax.xaxis.set_label_coords(.5, -.1)

隐藏轴线/边框：
Axis: https://matplotlib.org/stable/api/axis_api.html
Spines: https://matplotlib.org/stable/gallery/spines/spines.html
# Hide the right and top spines
ax1.spines.right.set_visible(False)
ax1.spines.top.set_visible(False)

同时隐藏坐标轴的ticks和数字：
1. plt.axis('off') 同时隐藏x, y轴
2. ax.get_xaxis().set_visible(False)
   ax.get_yaxis().set_visible(False)
3. ax.set_xticks([])

只修改/隐藏坐标轴上的ticks(小竖线)：
# Only show ticks on the left and bottom spines
ax1.yaxis.set_ticks_position('left')
ax1.xaxis.set_ticks_position('bottom') {'top', 'bottom', 'both', 'default', 'none'}

只隐藏坐标轴上的数字/文本
ax.set_ticklables([])

ax.set_xlim(l, r)  # 设置图像与两边的空隙 

坐标轴逆序：ax.invertxaxis() invertyaxis() 注意set_xlim等方法的左右要反过来

带断层的y轴 https://matplotlib.org/stable/gallery/subplots_axes_and_figures/broken_axis.html
broken barh https://matplotlib.org/stable/gallery/lines_bars_and_markers/broken_barh.html
ax.spines.left.set_bounds()
ax.spines.bottom.set_bounds()

添加新坐标轴：
plt.twinx() plt.twiny() https://matplotlib.org/stable/api/_as_gen/matplotlib.pyplot.twinx.html
https://matplotlib.org/stable/gallery/axisartist/simple_axisline.html
https://matplotlib.org/stable/gallery/axisartist/demo_parasite_axes.html

标题：plt.title()
子图标题：ax.set_title
子图总标题：fig.suptitle()


分别画多张图
plt.figure(1)
plt.plot()

plt.figure(2)
plt.plot()

子图：
https://matplotlib.org/2.0.2/api/pyplot_api.html#matplotlib.pyplot.subplots

subplots(nrows, ncols)

import matplotlib.axes._axes as axes
import matplotlib.figure as figure
fig, ax = plt.subplots()  # type:figure.Figure, axes.Axes
fig, (ax1, ax2) = subplots(1, 2, figsize=())
fig.suptitle()
ax1.plot()
ax2.plot()
ax1.set_xticks()
fig.savefig()
plt.clf()
plt.close(fig)

fig, axes = subplots(2,2)
axes[0, 1]
ax = axes.ravel()
ax[3]

可合并的子图：
plt.subplot(nrow,ncol,num)
如：
 ---------------
|    (2,1,1)    |
 ---------------
|(2,2,3)|(2,2,4)|
 ---------------
fig = plt.figure()
ax1=fig.add_subplot(2,1,1)
ax3=fig.add_subplot(2,2,3)
ax4=fig.add_subplot(2,2,4)

调整间距：
1. plt/fig.tight_layout() w_pad=0
   savefig(bbox_inches='tight', pad_inches=0)
2. plt.subplots_adjust(left=0.1, right=0.99, top=0.9, bottom=0.1, wspace, hspace)
   args: left, right, bottom, top, wspace, hspace. float. 0~1 表示比例，不包含坐标轴
3. plt.subplots(constrained_layout=True)

调整子图比例：
https://matplotlib.org/stable/tutorials/intermediate/arranging_axes.html
1. fig.suplots(gridspec_kw={'height_ratios': self.height_ratios, 'width_ratios':widths})
2. spec2 = gridspec.GridSpec(ncols=2, nrows=2, figure=fig2)
ax1 = fig2.add_subplot(spec2[0, 0])
ax2 = fig2.add_subplot(spec2[0, 1])
ax3 = fig2.add_subplot(spec2[1, 0])
ax4 = fig2.add_subplot(spec2[1, 1])
3. spec=fig.add_gridspec(ncols=2, nrows=3, width_ratios=[], height_ratios=[])
fig.add_subplot(spec[row,col])

字体字号：https://matplotlib.org/stable/tutorials/introductory/customizing.html
（写在代码最前面）
matplotlib.rcParams.update({})

字体： 
中文宋体+英文Times new roman: https://zhuanlan.zhihu.com/p/118601703
（解决中文乱码）
全局：
plt.rcParams['font.family'] = 'SimSun' #黑体["SimHei"]
plt.rcParams["axes.unicode_minus"]=False #正常显示负号
'weight':'bold',            # 粗细
参数：
	fontproperties='SimSun'
或
	fontdict=dict(family='SimSun')   #'Microsoft YaHei'

配合xelatex：
matplotlib.use('pgf')
plt.rcParams['pgf.preamble'] = preamble

英文字体 times：
#plt.rc('font', **{'family': 'serif', 'serif': ['Times'], 'size': font_size=12})
全局：plt.rcParams["font.family"] = "Times New Roman"
参数：fontdict=dict(family='Times New Roman')

字号 (pyplot 的字号使用pt）
三号16pt、小三15pt、四号14pt、小四12pt、五号10.5pt
plt.rcParams['font.size'] = 15  #全局
# 设置刻度字体大小
plt.xticks(fontsize=12)
plt.yticks(fontsize=12)
# 设置坐标标签字体大小
ax.set_xlabel(..., fontsize=12)
ax.set_ylabel(..., fontsize=12)
# 设置图例字体大小
ax.legend(..., fontsize=12)


使用latex：
https://matplotlib.org/stable/gallery/text_labels_and_annotations/tex_demo.html
plt.rcParams['text.usetex'] = True
plt.rc('text', usetex=True)
plt.rc('font', **{'family': 'serif', 'serif': ['Times'], 'size': font_size=8})
plt.rc('text.latex', preamble='\n'.join[r'\usepackage{amssymb}'])
matplotlib.verbose.level = 'debug-annoying'

r'text expression and $math expression$'

PDF:
嵌入文字，可以被编辑
plt.rcParams['pdf.fonttype'] = 42
plt.rcParams['ps.fonttype'] = 42

背景网格：https://matplotlib.org/stable/api/_as_gen/matplotlib.pyplot.grid.html
ax.grid(visible=True, which='major', axis='x'/'y'/'both', color='w')
ax.set_axisbelow(True)  画在里面

{{.\pasted_image.png}}
