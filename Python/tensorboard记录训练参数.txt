Content-Type: text/x-zim-wiki
Wiki-Format: zim 0.6
Creation-Date: 2024-09-27T20:08:48+08:00

====== tensorboard记录训练参数 ======
Created 星期五 27 九月 2024

pip install tensorboard

torch.utils.tensorboard.SummaryWriter
https://docs.pytorch.org/tutorials/recipes/recipes/tensorboard_with_pytorch.html
https://docs.pytorch.org/docs/stable/tensorboard.html

writer = SummaryWriter(log_dir='logs/run1') if not eval else None
writer.add_scalar('Loss/Train', loss, epoch)  # tag加斜杠可以用于分类
writer.flush()

.add_scalar
.add_scalars
.add_text
.add_image  # 输入tensor，如果dtype是np.float64（默认），则取值范围[0,1]；如果dtype是np.uint8，则取值范围[0,255]
.add_images
.add_figure # 输入plt的figure对象

# log image
# https://www.tensorflow.org/tensorboard/image_summaries#visualizing_a_single_image
.add_image(



cmd:
> tensorboard --logdir experiments --host 0.0.0.0 --samples_per_plugin "images=20"
http://localhost:6006/
# 根据logs下的不同文件夹，会自动识别成不同的run
# 默认只显示10张图片，修改 --samples_per_plugin 
