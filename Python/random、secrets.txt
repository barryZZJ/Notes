Content-Type: text/x-zim-wiki
Wiki-Format: zim 0.6

random.Random(seed).xxx 固定种子
.choices 选k次，故有重复
.choice
.sample 选一次，一次k个，无重复
.randrange(start, stop[, step]) -> int
.randint(a, b) -> [a,b] int == randrange(a, b+1)
.randbytes
.shuffle
	To shuffle an immutable sequence and return a new shuffled list, use sample(x, k=len(x)) instead.

secrets 保障了安全性的随机数
https://docs.python.org/3/library/secrets.html
.choice
.randbelow
.randbits
.token_bytes
.token_hex
.token_urlsafe
