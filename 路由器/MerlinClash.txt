Content-Type: text/x-zim-wiki
Wiki-Format: zim 0.6
Creation-Date: 2022-10-07T17:27:46+08:00

====== MerlinClash ======
Created 星期五 07 十月 2022

帮助文档
https://mcreadme.gitbook.io/mc/

/koolshare/merlinclash

插件下载
//质壁分离版：在线安装内核+在线规则转换+在线控制面板
//Lite版：质壁分离总是下载内核失败的使用
ARM v7 386（旧版）
ARM  V7 AX32（新版，优先选择）
Lite版本同AX32合并，小JFFS机型请直接安装AX32版本，安装脚本会根据机型选择安装功能。（但经过测试每隔10分钟就会触发路由器重启，用不了，可能是订阅+更新geoip之类的之后总空间超了），
只要删掉/jffs/uu.tar.gz就有空间装10M的版本了。

安装/重装：
选择.tar.gz文件后点击上传并安装
更新需要保证jfss有5M以上剩余空间
如果空间不足可以删掉/jffs里的uu.tar.gz（每次重启都会新建出来）

（需要在/koolshare/merlinclash里mkdir dashboard

安装补丁：
选择.tar.gz文件后点击上传并安装
热更补丁文件名为 patch-AAAA-BBB.tar.gz。AAAA是插件版本，BBB是补丁序号；
补丁需要与插件版本对应才能正常更新，既AAAA必须与MC本体版本相同；
如无特殊说明，最新补丁包含之前所有版本的内容

初始化hosts：
hosts:
#开启MC后，可使用域名登陆路由器后台
  router.asus.com: 192.168.50.1
  #google中国重新定向
  services.googleapis.cn: 74.125.193.94
  #win网络状态检查
  ipv6.msftconnecttest.com: 2a01:111:2003::52
  www.msftconnecttest.com: 13.107.4.52

连接配置页面：设置端口转发

出错连不上了先去配置页面点一遍各种重建按钮

faq
https://mcreadme.gitbook.io/mc/faq/faq1#q17

连不上clash管理面板：见faq，需要改chrome的flag：
chrome://flags/#Insecure-origins-treated-as-secure
enable: ''http://yacd.haishan.me,http://clash.razord.top,http://clash.metacubex.one,http://yacd.metacubex.one,http://router.asus.com''
如果还是连不上，点网址左边的按钮——网站设置——不安全内容设为允许

规则匹配序号在前的优先（自定义规则中约晚加入的越早匹配）

常用后端地址：
可能是后端通信遇到问题，尝试更换后端，常用后端地址：
   https://v.id9.cc/                                （推荐）
   https://sub.maoxiongnet.com/         （推荐）
   https://sub.xeton.dev/
   https://api.tshl.us/ 
   https://sub.id9.cc/
   https://subconverter.herokuapp.com/
   https://api.dler.io/
   https://api.wcc.best/

自定义规则解码：
rule_parser.py
{{{code: lang="python3" linenumbers="False"
import base64
from urllib.parse import unquote, quote


def decode(line) -> str:
    parts = []
    for part in line.split(','):
        decoded = unquote(base64.b64decode(part))
        parts.append(decoded)
    return ','.join(parts)

def encode(line) -> str:
    parts = []
    for part in line.split(','):
        encoded = base64.b64encode(quote(part).encode()).decode()
        parts.append(encoded)
    return ','.join(parts)


def to_decoded(file):
    f1 = open(file, 'r', encoding='utf8')
    res = []
    for line in f1.readlines():
        line = line.strip()
        decoded = decode(line)
        print(decoded)
        res.append(decoded)
        res.append('\n')
    f1.close()
    
    f2 = open(file, 'w', encoding='utf8')
    f2.writelines(res)
    f2.close()

def to_encoded(file):
    f1 = open(file, 'r', encoding='utf8')
    res = []
    for line in f1.readlines():
        line = line.strip()
        encoded = encode(line)
        print(encoded)
        res.append(encoded)
        res.append('\n')
    f1.close()
    
    f2 = open(file, 'w', encoding='utf8')
    f2.writelines(res)
    f2.close()
}}}

encode/decode:
{{{code: lang="python3" linenumbers="False"
#encode:
from rule_parser import to_encoded

infile = input('custom_rule.yaml to be encoded:\n>')

to_encoded(infile)

# decode:
from rule_parser import to_decoded

infile = input('custom_rule.yaml to be decoded:\n>')

to_decoded(infile)
}}}
