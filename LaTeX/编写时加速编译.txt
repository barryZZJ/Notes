Content-Type: text/x-zim-wiki

1. documentdclass添加draft参数：不渲染图片

2. \includeonly{} 只渲染括号中的文件，与\include{}或\input{}搭配使用

3. 最后介绍一个实用的工具宏包 syntonly。加载这个宏包后，在导言区使用 \syntaxonly 命
令，可令 LATEX 编译后不生成 DVI 或者 PDF 文档，只排查错误，编译速度会快不少：
\usepackage{syntonly}
\syntaxonly
如果想生成文档，则用 % 注释掉 \syntaxonly 命令即可。